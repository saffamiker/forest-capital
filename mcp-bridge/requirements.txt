# mcp-bridge runtime dependencies.
#
# Kept intentionally small — sqlite3 is stdlib so no DB driver,
# the MCP wire protocol is implemented directly on top of FastAPI
# (no separate mcp SDK), and the worker shells out to the claude
# CLI rather than embedding the Anthropic SDK.

fastapi>=0.110
uvicorn[standard]>=0.27
# structlog is used by the worker for structured event lines.
# Optional — the worker falls back to print() when structlog
# isn't importable, so a minimal install can drop this line.
structlog>=24.1

# Test dependencies (also pulled in by `pip install -e .[dev]`
# when we add the pyproject extras).
# Uncomment these or install via mcp-bridge/requirements-dev.txt:
# pytest>=8
# httpx>=0.27  # FastAPI TestClient under the hood
