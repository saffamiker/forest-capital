.env
backend/.env
data/cache/
venv/
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
node_modules/
dist/
build/
.vite/
*.parquet
.DS_Store
*.log

.vercel
.env*.local

# MCP bridge connector config — carries the bearer token that gates
# prompt execution on the desktop. Never commit it.
.mcp.json

# Analytical Appendix submission package artifacts -- regenerated by
# create_submission_package.py from the committed notebook + data.
# The .ipynb (with executed cell outputs) is the canonical source;
# .html / .zip / SUBMISSION_README.txt are derived and should not be
# committed.
analytical_appendix.html
forest_capital_analytical_appendix_*.zip
SUBMISSION_README.txt
